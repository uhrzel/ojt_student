// AttendanceScreen.dart

import 'package:flutter/material.dart';

class TaskScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('TaskScreen'),
      ),
      body: Center(
        child: Text('TaskScreen Screen Content'),
      ),
    );
  }
}
